import 'package:flutter/material.dart';

class IconHelper{
  static var addIcon = const Icon(Icons.add, color: Colors.white,);
  static var logoutIcon = const Icon(Icons.logout_outlined, color: Colors.white,);
}